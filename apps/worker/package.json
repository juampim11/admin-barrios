{
  "name": "@admin-barrios/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "El proceso que genera los documentos: consume la cola y renderiza los PDF (ADR-0002 §6.3)",
  "scripts": {
    "dev": "node --watch src/main.ts",
    "start": "node src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@admin-barrios/almacenamiento": "workspace:*",
    "@admin-barrios/auth": "workspace:*",
    "@admin-barrios/data": "workspace:*",
    "@admin-barrios/documentos": "workspace:*",
    "@admin-barrios/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.4",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
