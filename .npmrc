# Dependencias explícitas: un paquete solo puede importar lo que declara en su package.json.
# Evita que apps/web use por accidente algo que instaló packages/data (y viceversa).
hoist=false
strict-peer-dependencies=false
